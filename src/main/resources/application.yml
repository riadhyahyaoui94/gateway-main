spring:
  cloud:
    gateway:
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: "http://localhost:80"
            allowedHeaders: "*"
            allowCredentials: "true"
            allowedMethods:
              - GET
              - POST
              - DELETE
              - PUT
